import Scrape from "./Scrape.js";

/**
 * Using if html main does not work.
 * @returns {JSX.Element}
 */
function Main() {
    return (
        <div className="App">
            <Scrape> </Scrape>
         </div>
    );
}

export default Main;
